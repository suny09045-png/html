# 🚀 我的网站 - 登录系统搭建指南

## 📋 目录

- [快速开始](#快速开始)
- [修改清单](#修改清单)
- [详细修改说明](#详细修改说明)
- [文件结构](#文件结构)
- [常见问题](#常见问题)

---

## ⚡ 快速开始

### 环境要求
- Node.js 14+ (推荐 v16 或 v18)
- npm (随 Node.js 安装)
- 现代浏览器 (Chrome/Firefox/Edge)

### 一键启动

```bash
# 进入项目目录
cd /workspace

# 安装依赖 (如果有额外依赖)
# npm install

# 启动服务器
node server.js
```

然后打开浏览器访问: **http://localhost:3000**

### 测试账号
| 用户名 | 密码 |
|--------|------|
| admin | 123456 |

---

## 📝 修改清单

如果你想修改成自己的网站，需要修改以下文件：

| 序号 | 文件路径 | 修改内容 | 优先级 |
|------|----------|----------|--------|
| 1 | `server.js` | 测试账号密码、端口号 | 🔴 高 |
| 2 | `configuration.js` | 站点标题、名称 | 🔴 高 |
| 3 | `user_login.js` | 登录跳转地址 | 🟡 中 |
| 4 | `login.html` | 页面标题、样式 | 🟡 中 |
| 5 | `common.js` | 通知开关 | 🟢 低 |
| 6 | `main.html` | 后台页面 | 🟢 低 |

---

## 📖 详细修改说明

### 1️⃣ 修改账号密码 (`server.js`)

**位置**: 第 15-16 行

```javascript
const TEST_USERS = {
    'admin': '123456'   // ← 修改为你的账号:密码
};
```

**添加多个用户**:
```javascript
const TEST_USERS = {
    'admin': '123456',
    'user1': 'password1',
    'test': 'test123'
};
```

**修改端口号**: 第 5 行
```javascript
const PORT = 3000;  // ← 修改为你想要的端口
```

---

### 2️⃣ 修改站点配置 (`configuration.js`)

**位置**: 第 7-27 行

| 变量名 | 用途 | 示例值 |
|--------|------|--------|
| `global_title` | 网站标题 | `"我的商城"` |
| `global_appname` | 应用名称 | `"MyApp"` |
| `global_skin` | 皮肤主题 | `"blue"` |
| `global_balancename` | 余额名称 | `"余额"` |
| `global_currencyname` | 积分名称 | `"积分"` |
| `global_integralname` | 金币名称 | `"金币"` |

**示例 - 改成游戏网站**:
```javascript
var global_title = "🎮 游戏中心";
var global_appname = "GameCenter";
var global_balancename = "游戏币";
var global_currencyname = "钻石";
var global_integralname = "经验值";
```

---

### 3️⃣ 修改登录跳转地址 (`user_login.js`)

**位置**: 第 49 行

```javascript
location.href = '/template/num001/main.html?mid=' + mid;
// 改成你实际的后台页面地址
```

---

### 4️⃣ 修改登录页面 (`login.html`)

**标题**: 第 5 行
```html
<title>我的网站 - 控制台登录</title>
```

**网站名称**: 第 166 行
```html
<span class="site-name" id="siteTitle">我的网站</span>
```

**页脚信息**: 第 191 行
```html
2025 我的网站 · All Rights Reserved
```

**主题颜色**: 第 28 行 (CSS)
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
/* 渐变色起点色  → 渐变色终点色 */
```

**常用配色方案**:
```css
/* 蓝色系 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* 绿色系 */
background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);

/* 橙红色系 */
background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);

/* 深蓝系 */
background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);

/* 紫色系 */
background: linear-gradient(135deg, #7f00ff 0%, #e100ff 100%);
```

---

### 5️⃣ 关闭通知弹窗 (`common.js`)

**位置**: 第 4 行

```javascript
var NOTICE_ENABLED = false;  // true = 开启, false = 关闭
```

---

### 6️⃣ 修改后台页面 (`main.html`)

修改 `template/num001/main.html` 文件中的：
- 第 5 行: 页面标题
- 第 8-9 行: 统计数据
- 第 15-16 行: Logo 和网站名称
- 其他功能按钮和内容

---

## 📁 文件结构

```
/workspace/
├── server.js                    # 本地服务器 (必须运行)
├── login/                       # 前端静态文件
│   ├── zpx.belmorian.com/       # 网站主目录
│   │   ├── _admin/
│   │   │   ├── login.html       # 登录页面 ← 修改这里
│   │   │   ├── jsmethod/
│   │   │   │   └── user_login.js # 登录逻辑 ← 修改这里
│   │   │   └── js/              # JS 库
│   │   │       ├── common/      # 公共库
│   │   │       ├── jquery-core.min.js
│   │   │       └── fai.min.js
│   │   │   └── layui/           # UI 框架
│   │   ├── public/
│   │   │   └── configuration.js # 全局配置 ← 修改这里
│   │   └── user/
│   │       └── systemconfig.html
│   ├── notice.flashgo.cc/
│   │   └── js/
│   │       └── common.js        # 通知系统 ← 可选修改
│   └── _DataURI/
└── template/
    └── num001/
        └── main.html            # 后台主页 ← 修改这里
```

---

## 🔧 高级配置

### 添加新的 API 接口

在 `server.js` 中添加：

```javascript
if (pathname === '/api/your-api' && method === 'POST') {
    const { param1, param2 } = postData;

    // 处理逻辑
    const response = {
        state: 1,
        data: { /* 返回数据 */ }
    };

    res.writeHead(200, { 'Content-Type': 'application/json' });
    res.end(JSON.stringify(response));
    return;
}
```

### 添加新页面

1. 创建 HTML 文件 (如 `page.html`)
2. 在 `server.js` 中确保文件能被访问
3. 在登录后跳转或添加菜单链接

---

## ❓ 常见问题

### Q1: 启动报错 "port already in use"

端口被占用，修改 `server.js` 中的端口：

```javascript
const PORT = 3001;  // 改成其他端口
```

### Q2: 登录成功但不跳转

检查 `user_login.js` 第 49 行的跳转地址是否正确

### Q3: CSS 样式不生效

清除浏览器缓存，或使用 Ctrl+F5 强制刷新

### Q4: 提示 "cookie 未启用"

浏览器需要开启 Cookie 支持才能正常登录

### Q5: 如何添加更多测试账号？

修改 `server.js` 第 15-16 行：

```javascript
const TEST_USERS = {
    'admin': '123456',
    'user1': 'password1',  // 添加新账号
    'user2': 'password2'
};
```

---

## 🌐 部署到生产环境

### 使用 Nginx

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        root /path/to/workspace/login/zpx.belmorian.com/_admin;
        index login.html;
    }

    location /admin/login {
        proxy_pass http://localhost:3000/admin/login;
    }

    location /user/systemconfig {
        proxy_pass http://localhost:3000/user/systemconfig;
    }
}
```

### 使用 PM2 守护进程

```bash
# 安装 PM2
npm install -g pm2

# 启动服务
pm2 start server.js --name mysite

# 设置开机自启
pm2 startup
pm2 save

# 查看状态
pm2 list
pm2 logs mysite
```

---

## 📞 获取帮助

如有问题，请检查：
1. Node.js 是否正确安装 (`node -v`)
2. 端口是否被占用 (`lsof -i :3000`)
3. 文件路径是否正确

祝开发愉快！🎉
